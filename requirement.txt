
# Tarizz - Project Manager with encrypted diary
# Python 3.7+

# GUI is built with tkinter (stdlib) + Pillow for images
Pillow>=9.0.0

# OpenCV for video thumbnail / media handling
opencv-python>=4.5.0

# PDF rendering (PyMuPDF) + PDF export
PyMuPDF>=1.20.0
reportlab>=3.6.0

# Cryptography for password-protected vaults / encryption
cryptography>=3.4.0