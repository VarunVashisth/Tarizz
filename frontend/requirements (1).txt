# requirements.txt — Tarizz
# ─────────────────────────
# Already required by your frontend (no version change needed):
Pillow>=10.0
PyMuPDF>=1.22          # fitz
opencv-python>=4.8     # cv2

# New — required by the backend:
cryptography>=41.0     # AES-256-GCM, scrypt (via hashlib uses this under the hood for the C extension)
# Note: hashlib.scrypt is stdlib (Python 3.6+); no extra package needed for key derivation.
# SQLite3 is also stdlib.  No additional DB package required.
reportlab>=4.4.10